: | # 饿了么自动报名活动 - 环境变量配置
# 复制此文件为 .env 并填写实际值

# 饿了么活动页面URL
ELEME_ACTIVITY_URL=https://nr.ele.me/app/eleme-nr-bfe-newretail/common-next#/pc/activityCenter

# 门店名称（多个用逗号分隔）
STORE_NAMES=Oby便利超市(安吉店),Oby便利超市(长兴店)

# 飞书Webhook（用于通知）
FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/your-webhook-token

# 截图保留天数
KEEP_DAYS=7

# 图像识别阈值（0-1，越高越严格）
MATCH_THRESHOLD=0.8

# Playwright配置
# true=无头模式（不显示浏览器），false=有头模式（显示浏览器）
PLAYWRIGHT_HEADLESS=false

# 截图保存目录
SCREENSHOT_DIR=screenshots

# 日志文件
LOG_FILE=activity_signup.log
